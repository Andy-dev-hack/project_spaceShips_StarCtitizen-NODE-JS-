/
├── .env                              # 1. Variables de entorno (secretos: MONGO_URI, PORT)
├── .gitignore                        # 2. Ignora /node_modules y .env
├── package.json
├── server.js                         # 3. Punto de entrada (Inicializa Express, DB, y Middlewares)
│
├── config/
│   └── db.js                         # 4. Conexión a MongoDB (Usa MONGO_URI)
│
├── middleware/
│   ├── auth.middleware.js            # 5. Autenticación (Verifica Bearer Token, simula '123')
│   └── isRole.middleware.js          # 6. Autorización (Verifica req.user.role)
│
├── routes/
│   ├── naves.routes.js               # 7. Rutas para /naves (GET, POST, PUT protegido)
│   └── patrol.routes.js              # 8. Rutas para /patrol (GET, POST, PUT protegido)
│
├── |
│   ├── controllers/
│   │   ├── naves.controller.js       # 9. Controla peticiones HTTP para Naves (Usa servicios)
│   │   └── patrol.controller.js      # 10. Controla peticiones HTTP para Patrullas (Usa servicios)
│   │
│   ├── models/
│   │   ├── Nave.model.js             # 11. Esquema Mongoose para Naves
│   │   └── Patrol.model.js           # 12. Esquema Mongoose para Patrullas
│   │
│   └── services/
│       ├── naves.service.js          # 13. Lógica de negocio/DB para Naves (Usa modelo)
│       └── patrol.service.js         # 14. Lógica de negocio/DB para Patrullas (Usa modelo)